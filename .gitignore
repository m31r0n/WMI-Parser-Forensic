# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Claude Code local settings (permissions, personal config)
.claude/

# Virtual environments
.venv/
venv/
env/

# WMI evidence files — never commit real forensic artefacts to public repos
OBJECTS.DATA
OBJECTS.data
MAPPING*.MAP
MAPPING*.map
INDEX.BTR
INDEX.btr

# Tool output files (reports, triage exports, carved hits)
output/
*.xls
*.xlsx
*.tsv
report.json
report.txt
triage*.csv
class-hits*.json

# Auto-named reports and extracted payloads (default to the evidence folder)
wmi_*.xlsx
wmi_*_payloads_*/

# Recovered/extracted binaries — NEVER commit dumped malware to a public repo
*.exe
*.dll
*.bin

# OS
.DS_Store
Thumbs.db
